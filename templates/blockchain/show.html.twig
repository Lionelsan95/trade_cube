{% extends 'base.html.twig' %}

{% block title %}Blockchain{% endblock %}

{% block body %}
    <h1>Blockchain</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ blockchain.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ blockchain.name }}</td>
            </tr>
            <tr>
                <th>Cle_api</th>
                <td>{{ blockchain.cleApi }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('blockchain_index') }}">back to list</a>

    <a href="{{ path('blockchain_edit', {'id': blockchain.id}) }}">edit</a>

    {{ include('blockchain/_delete_form.html.twig') }}
{% endblock %}
